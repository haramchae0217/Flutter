void main() {
  /// 함수 / 메서드
  /// 프로그래밍에서 가장 기본이 되는 단위 중 하나
  /// 동작에 대한 단위
  /// 리턴 다입 함수명 (매개변수) {
  ///   함수 내 동작 할 코드
  /// }
  int num1 = 10;
  int num2 = 20;

  int sum = add(num1, num2);
}

int add(int num1, int num2) {
  return num1 + num2;
}